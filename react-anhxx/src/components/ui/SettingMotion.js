import React, {useRef, useEffect} from 'react';

function SettingMotion({cName}){
    console.log('on setting Motion');
    const modernElm = useRef(null);

    useEffect(() => {
       
    }, []);
    
    return (
        <>
        </>
    );
}

export default SettingMotion;
